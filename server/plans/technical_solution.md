# Техническое решение: Корпоративный Мессенджер

## 1. Введение
Данный документ описывает техническую реализацию проекта корпоративного мессенджера. Архитектура системы построена на основе микросервисного подхода (или сервисно-ориентированной архитектуры), что обеспечивает масштабируемость, гибкость разработки и изоляцию компонентов.

Основа архитектуры — диаграмма компонентов (`component.puml`).

## 2. Стек технологий

### Backend
*   **Язык программирования**: **Go (Golang)**.
    *   *Причина выбора*: Высокая производительность, отличная поддержка конкурентности (goroutines) — критически важно для обработки множества одновременных соединений в чатах (WebSocket). Строгая типизация и компилируемость обеспечивают надежность кода.
*   **Фреймворки/Библиотеки**:
    *   `net/http` (стандартная библиотека) или `Gin`/`Echo` для REST API.
    *   `gorilla/websocket` или `nhooyr.io/websocket` для реализации чатов в реальном времени.
    *   `pgx` или `GORM` для работы с базой данных.

### База данных
*   **СУБД**: **PostgreSQL**.
    *   *Причина выбора*: Надежная реляционная база данных, соответствующая сложной схеме данных проекта (связи между пользователями, задачами, чатами и рабочими пространствами). Поддержка ACID транзакций.

### Инфраструктура и Деплой
*   **Контейнеризация**: **Docker**.
    *   Каждый сервис и база данных упаковываются в отдельные контейнеры.
*   **Оркестрация**: **Docker Compose**.
    *   *Причина выбора*: Удобство локальной разработки и развертывания. Позволяет описать всю инфраструктуру (сервисы, БД, сети, тома) в одном файле `docker-compose.yml`.

### Протоколы взаимодействия
*   **REST (HTTP/JSON)**: Для синхронного взаимодействия (CRUD операции с задачами, пользователями, настройками).
*   **WebSocket**: Для асинхронного обмена сообщениями в реальном времени (чат).

## 3. Архитектура системы

Система состоит из следующих ключевых компонентов (согласно диаграмме компонентов):

### 3.1. API Gateway
*   **Роль**: Единая точка входа для всех клиентов (Web, Desktop).
*   **Реализация**: Сервис на Go (или Nginx).
*   **Функции**:
    *   Маршрутизация запросов к соответствующим микросервисам.
    *   Предварительная проверка авторизации (валидация JWT).
    *   Агрегация ответов (опционально).

### 3.2. Сервисы Бизнес-логики (Backend Services)

Каждый сервис реализуется как отдельное Go-приложение.

1.  **Сервис Аутентификации (Auth Service)**
    *   Регистрация, вход, выдача JWT токенов (Access/Refresh).
    *   Хранение хешей паролей.

2.  **Сервис Пользователей (User Service)**
    *   Управление профилями, статусами.
    *   Поиск пользователей.

3.  **Сервис Рабочих пространств (Workspace Service)**
    *   CRUD рабочих пространств.
    *   Управление ролями пользователей в рамках пространства.
    *   Проверка прав доступа.

4.  **Сервис Чатов и Сообщений (Chat & Message Service)**
    *   **Ключевой компонент**.
    *   Управление чатами (создание, удаление, участники).
    *   Обработка сообщений через WebSocket.
    *   Сохранение истории сообщений в БД.

5.  **Сервис Задач (Task Service)**
    *   CRUD задач (создание, редактирование, статус).
    *   Назначение исполнителей.
    *   Связь задач с чатами.

6.  **Сервис Жалоб (Complaint Service)**
    *   Прием и обработка жалоб от пользователей.

### 3.3. Слой данных (Data Access Layer)
*   Сервисы обращаются к единой (или разделенной по схемам) базе данных PostgreSQL.
*   Использование миграций (например, `golang-migrate`) для управления схемой БД.

## 4. Структура Docker Compose

Примерная структура `docker-compose.yml`:

```yaml
version: '3.8'
services:
  # База данных
  postgres:
    image: postgres:15-alpine
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: messenger_db

  # Сервисы (пример для одного, остальные аналогично)
  auth-service:
    build: ./services/auth
    ports:
      - "8081:8080"
    environment:
      - DB_HOST=postgres
    depends_on:
      - postgres

  chat-service:
    build: ./services/chat
    environment:
      - DB_HOST=postgres
    depends_on:
      - postgres

  # API Gateway
  gateway:
    build: ./gateway
    ports:
      - "8080:8080" # Единый порт наружу
    depends_on:
      - auth-service
      - chat-service
      # ... остальные сервисы

volumes:
  db_data:
```

## 5. REST API Спецификация

Подробное описание всех REST API эндпоинтов для каждого микросервиса находится в отдельной папке:

**📁 [API Documentation](./api/README.md)**

Документация разбита по микросервисам:
- [API Gateway](./api/gateway.md) - маршрутизация и валидация
- [Auth Service](./api/auth_service.md) - аутентификация и JWT
- [User Service](./api/user_service.md) - профили и статусы
- [Workspace Service](./api/workspace_service.md) - рабочие пространства
- [Chat Service](./api/chat_service.md) - чаты и WebSocket
- [Task Service](./api/task_service.md) - задачи и исполнители
- [Complaint Service](./api/complaint_service.md) - жалобы пользователей

### Краткая сводка эндпоинтов:

| Сервис | Порт | Основные эндпоинты | Кол-во |
|--------|------|-------------------|--------|
| **API Gateway** | 8080 | Маршрутизация, health check | 1 |
| **Auth Service** | 8081 | `/auth/register`, `/auth/login`, `/auth/refresh` | 6 |
| **User Service** | 8082 | `/users/me`, `/users/:id`, `/users/search`, `/users/workspace/:id` | 7 |
| **Workspace Service** | 8083 | `/workspaces`, `/workspaces/:id/members`, `/tariffs` | 12 |
| **Chat Service** | 8084 | `/chats`, `/chats/:id/messages`, WebSocket | 15 |
| **Task Service** | 8085 | `/tasks`, `/tasks/:id/assignees`, `/tasks/:id/status` | 13 |
| **Complaint Service** | 8086 | `/complaints`, `/complaints/:id/status` | 5 |

**Итого: 58 REST эндпоинтов + 1 WebSocket соединение**

**Статус реализации**: 29/58 эндпоинтов (50%)
- ✅ Auth Service: 7/7 эндпоинтов
- ✅ User Service: 7/7 эндпоинтов
- ✅ Workspace Service: 12/12 эндпоинтов
- ✅ Chat Service: 15/15 эндпоинтов + WebSocket
- ⬜ Остальные сервисы: 0/18 эндпоинтов

## 6. План разработки (Roadmap)

1.  **Проектирование БД**: Создание SQL-схемы на основе ER-диаграммы.
2.  **Базовая инфраструктура**: Настройка Docker Compose и репозитория.
3.  **Auth Service**: Реализация регистрации и логина.
4.  **Gateway**: Настройка проксирования и валидации JWT.
5.  **Workspace & User Services**: Базовая логика пользователей и пространств.
6.  **Chat Service**: Реализация WebSocket и обмена сообщениями.
7.  **Task Service**: Управление задачами.
8.  **Complaint Service**: Обработка жалоб.
9.  **Интеграция**: Сборка всего в единый контур.
10. **Тестирование**: Unit, интеграционные и E2E тесты.
